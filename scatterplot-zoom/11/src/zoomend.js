// import { drawVoronoiPaths } from './drawVoronoiPaths';

export function zoomend(vis) {
  console.log('zoomend function called');
  console.log('vis object from inside of zoomend', vis);
  // d3.selectAll('.voronoi')
  //   .remove();

  // drawVoronoiPaths(vis, vis.exemplarData);
}
